# Changelog — docs/v2 + playbooks/v2

## 2026-04-20

### Added

- Initial **V2 truth documentation** tree under `docs/v2/`.
- **V2 playbooks** under `playbooks/v2/` with link map.
- **V3 playbooks** (“Annex” creative layer) under `playbooks/v3/` with separate link map.
- Companion `docs/v3/` notes describing the relationship between V2 truth and V3 narrative.

### Notes

- V1 playbook HTML files under `playbooks/` remain unchanged.
- Repository `.gitignore` continues to exclude large local mirrors (`v6-workspace/`, bridge `n8n_workflows/` at repo root when present).
